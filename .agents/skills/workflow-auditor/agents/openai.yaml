interface:
  display_name: "Workflow Auditor"
  short_description: "Auditar cambios Workflow 2.1 independientemente"
  default_prompt: "Usa $workflow-auditor para auditar este cambio contra el contrato aprobado, el diff real y la evidencia, sin modificar archivos."

policy:
  allow_implicit_invocation: false
