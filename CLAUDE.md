# Claude Code instructions

<!-- workflow-2:begin -->
@AGENTS.md

Use the project subagents under `.claude/agents/` for the Workflow 2.1 roles.
Use the `workflow-2-claude` skill for the reusable microcut workflow. Do not
duplicate repository policy in Claude-specific files; repository policy lives
in `AGENTS.md` and `.agents/`.
<!-- workflow-2:end -->
